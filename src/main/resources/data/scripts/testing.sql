-- Arquivo de script SQL pra testes de select, insert, update e delete no banco de dados.

-- START TRANSACTION;

-- SAVEPOINT samplesavepoint;

-- SELECT * FROM cleanControl.enterprise_dashboard_view

-- ROLLBACK TO SAVEPOINT samplesavepoint;
-- COMMIT;